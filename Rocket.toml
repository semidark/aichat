[default]
# Basic Rocket configuration
address = "0.0.0.0"
port = 8000
workers = 4
keep_alive = 5
log_level = "normal"

# Streaming configuration for Kindle e-ink optimization
[default.streaming]
delay_ms = 1000       # Milliseconds delay between chunks for e-ink displays

# Development profile
[debug]
address = "0.0.0.0"
port = 8000
workers = 1
keep_alive = 5
log_level = "normal" # Debug

[debug.streaming]
delay_ms = 1000

# Production profile
[release]
address = "0.0.0.0"
port = 8000
workers = 8
keep_alive = 10
log_level = "critical"

[release.streaming]
delay_ms = 300 